class DisplayCoin {
  final String name, tag, icon;
  final num price, gain;
  const DisplayCoin(
      {required this.name,
      required this.tag,
      required this.icon,
      required this.price,
      required this.gain});
}
